# pyproject.toml
[project]
name = "AI_POWERED_BOOKKEEPING"
version = "0.1.0"
description = "智能账单管理系统"
dependencies = [
    "fastapi",
    "sqlalchemy",
    "pydantic",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "app.*"]